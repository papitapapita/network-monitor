Create network devices

Verify what you need of every existing components
Is address part of the create devices? Or inside the device inventory bounded context?
How deep should I go for this Address implementation?

For this first sprint, are you validating that the city/province actually exist (against a list or external API), or just accepting any string the user provides?
No because this first clients will be held only in one province
Should the address be immutable once assigned to a device, or can operators update it freely?

Implement a module that saves the backups of the main devices like access points and routers automatically every certain time

To Do 6/4/2026
Make the backend run

Normalize the timestamps for proper logging use a NTPS

- Update the README.md
